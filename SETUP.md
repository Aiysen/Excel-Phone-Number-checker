# Пошаговая инструкция по настройке и запуску

## Этап 1: Подготовка окружения

### 1.1 Установка Node.js

1. Скачайте и установите Node.js LTS (версия 18 или выше) с официального сайта: https://nodejs.org/
2. Проверьте установку:
```bash
node --version
npm --version
```

### 1.2 Клонирование/подготовка репозитория

Если репозиторий уже есть локально, переходите к следующему этапу.

Если нужно создать новый репозиторий:
```bash
git init
git add .
git commit -m "Initial commit"
```

## Этап 2: Настройка Google таблицы

Бот работает только с **публичными** Google таблицами (доступными всем по ссылке).

### 2.1 Проверка публичного доступа

1. Откройте вашу Google таблицу:
   https://docs.google.com/spreadsheets/d/1LXGrv9ZG8QbkDwJ-cNMeiJj0JaIUGoWve47DEnFkizs/edit

2. Нажмите кнопку **"Настройки доступа"** (Share) в правом верхнем углу

3. Убедитесь, что установлен доступ:
   - **"Все, у кого есть ссылка"** или
   - **"Все в интернете"**

4. Если доступ ограничен — измените настройки на публичный доступ

### 2.2 Формат таблицы

Убедитесь, что таблица имеет правильный формат:

- **Один столбец** с данными
- **Первая строка** — заголовок `Phone`
- **Начиная со второй строки** — номера телефонов, только цифры (например: `79000423366`)

Пример:
```
Phone
79000423366
79000600652
79000871466
```

## Этап 3: Создание Telegram-бота

### 3.1 Получение Bot Token

1. Откройте Telegram и найдите бота **@BotFather**
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "Phone Number Checker")
   - Введите username бота (должен заканчиваться на `bot`, например: `phone_checker_bot`)
4. BotFather выдаст вам **токен** вида: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`
5. **Скопируйте и сохраните этот токен** — он понадобится для переменной `BOT_TOKEN`

### 3.2 Опционально: Настройка команд бота

В BotFather можно настроить команды:
```
/start - Начать работу с ботом
/help - Получить справку
```

Но это необязательно, команды уже реализованы в коде.

## Этап 4: Настройка переменных окружения

### 4.1 Создание файла .env

1. В корне проекта создайте файл `.env` (без расширения)

2. Заполните `.env` следующим содержимым:

```bash
BOT_TOKEN=ваш_токен_от_BotFather
GOOGLE_SHEET_ID=1LXGrv9ZG8QbkDwJ-cNMeiJj0JaIUGoWve47DEnFkizs
CACHE_TTL_MINUTES=10
PORT=3000
```

### 4.2 Важные моменты при заполнении .env

**BOT_TOKEN**: Вставьте токен, полученный от BotFather

**GOOGLE_SHEET_ID**: ID таблицы из URL (уже указан в примере)

**CACHE_TTL_MINUTES**: Интервал обновления кэша в минутах (по умолчанию 10)

**PORT**: Порт для HTTP-сервера (по умолчанию 3000, на Railway будет задан автоматически)

### 4.3 Проверка .gitignore

Убедитесь, что файл `.env` добавлен в `.gitignore` (он уже должен быть там):

```bash
# Проверьте содержимое .gitignore
cat .gitignore
```

Должна быть строка `.env`

## Этап 5: Установка зависимостей и локальный запуск

### 5.1 Установка зависимостей

```bash
npm install
```

Должны установиться пакеты:
- telegraf
- express
- dotenv

### 5.2 Запуск бота локально

```bash
npm start
```

Вы должны увидеть сообщения:
- `Инициализация кэша Google Sheets (публичный доступ)...`
- `✅ Кэш Google Sheets загружен, размер=XXX`
- `✅ Telegram бот запущен`
- `✅ HTTP сервер запущен на порту 3000`

### 5.3 Проверка работы

1. **Проверка health check**:
   - Откройте браузер и перейдите на `http://localhost:3000/health`
   - Должен вернуться JSON со статусом `ok` и информацией о кэше

2. **Проверка бота в Telegram**:
   - Найдите вашего бота по username
   - Отправьте команду `/start` — должно прийти приветствие
   - Отправьте команду `/help` — должна прийти справка
   - Отправьте номер телефона (например: `79000423366`) — должен прийти ответ о наличии/отсутствии номера

### 5.4 Тестирование нормализации номеров

Попробуйте отправить боту номера в разных форматах:
- `+7 900 042 33 66`
- `8 900 042 33 66`
- `9000423366`
- `79000423366`

Все они должны нормализоваться до `79000423366` и проверяться в таблице.

## Этап 6: Деплой на Railway

### 6.1 Подготовка репозитория

1. Убедитесь, что все изменения закоммичены:
```bash
git add .
git commit -m "Готово к деплою"
```

2. Создайте репозиторий на GitHub (если ещё нет):
   - Перейдите на https://github.com/new
   - Создайте новый репозиторий (можно приватный)
   - Следуйте инструкциям для push существующего репозитория

3. Запушьте код:
```bash
git remote add origin https://github.com/ваш_username/ваш_репозиторий.git
git branch -M main
git push -u origin main
```

### 6.2 Создание проекта на Railway

1. Перейдите на https://railway.app/
2. Войдите через GitHub
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Выберите ваш репозиторий
6. Railway автоматически определит Node.js проект

### 6.3 Настройка переменных окружения на Railway

1. В проекте Railway перейдите на вкладку **"Variables"**
2. Добавьте переменные:

   - **BOT_TOKEN**: ваш токен от BotFather
   - **GOOGLE_SHEET_ID**: `1LXGrv9ZG8QbkDwJ-cNMeiJj0JaIUGoWve47DEnFkizs`
   - **CACHE_TTL_MINUTES**: `10` (опционально)
   - **PORT**: Railway задаст автоматически, можно не указывать

### 6.4 Настройка деплоя

1. Railway автоматически определит команду запуска из `package.json` (`npm start`)
2. Если нужно настроить вручную:
   - Перейдите в **"Settings" → "Deploy"**
   - **Start Command**: `npm start`
   - **Build Command**: можно оставить пустым (или `npm install`)

### 6.5 Проверка деплоя

1. После деплоя Railway выдаст публичный URL (например: `https://your-project.railway.app`)
2. Проверьте health check:
   - Откройте `https://your-project.railway.app/health`
   - Должен вернуться JSON со статусом `ok`

3. Проверьте логи в Railway:
   - Перейдите на вкладку **"Deployments" → выберите последний деплой → "View Logs"**
   - Должны быть сообщения:
     - `Инициализация кэша Google Sheets (публичный доступ)...`
     - `✅ Кэш Google Sheets загружен, размер=XXX`
     - `✅ Telegram бот запущен`
     - `✅ HTTP сервер запущен на порту XXXX`

4. Проверьте бота в Telegram:
   - Отправьте `/start` — должно работать
   - Отправьте номер телефона — должна быть проверка

### 6.6 Настройка домена (опционально)

Если нужно использовать свой домен:
1. В Railway перейдите в **"Settings" → "Networking"**
2. Нажмите **"Generate Domain"** или добавьте свой домен
3. Обновите настройки, если нужно

## Этап 7: Проверка работоспособности

### 7.1 Функциональные проверки

- [ ] Команда `/start` работает
- [ ] Команда `/help` работает
- [ ] Проверка существующего номера возвращает `✅ Номер найден`
- [ ] Проверка несуществующего номера возвращает `❌ Номер не найден`
- [ ] Некорректный формат возвращает `⚠️ Некорректный формат номера`
- [ ] Нормализация номеров работает (`+7`, `8`, `900...` → `7900...`)

### 7.2 Проверка кэша

- [ ] Health check endpoint возвращает информацию о кэше
- [ ] Кэш обновляется по таймеру (можно проверить через `/health`, поле `lastUpdated`)
- [ ] При недоступности таблицы бот продолжает работать со старым кэшем

### 7.3 Проверка обработки ошибок

- [ ] При временной недоступности таблицы бот отвечает `🔴 Сервис временно недоступен`
- [ ] Бот не падает при ошибках, продолжает работать

## Возможные проблемы и решения

### Проблема: "Error: HTTP 403"

**Решение**: Проверьте, что:
- Таблица действительно публичная (доступна всем по ссылке)
- Настройки доступа установлены на "Все, у кого есть ссылка" или "Все в интернете"

### Проблема: "Error: HTTP 404"

**Решение**: Убедитесь, что:
- `GOOGLE_SHEET_ID` указан правильно (ID из URL таблицы)
- Таблица существует и доступна

### Проблема: Бот не отвечает

**Решение**: Проверьте:
- Правильность `BOT_TOKEN`
- Логи в Railway (если деплой) или в консоли (если локально)
- Что бот запущен и работает

### Проблема: "Номер не найден" для существующего номера

**Решение**: Проверьте:
- Формат номеров в таблице (должны быть только цифры)
- Что кэш загрузился (проверьте `/health`)
- Что нормализация работает правильно

### Проблема: Кэш не обновляется

**Решение**: Проверьте:
- Что `CACHE_TTL_MINUTES` установлен правильно
- Логи на наличие ошибок обновления кэша
- Что таблица доступна по публичной ссылке

## Дополнительные ресурсы

- [Документация Telegraf](https://telegraf.js.org/)
- [Документация Railway](https://docs.railway.app/)
- [Google Sheets](https://www.google.com/sheets/about/)
