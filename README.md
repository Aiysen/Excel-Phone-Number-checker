## Telegram-бот для проверки номера телефона

Бот принимает номер телефона от пользователя, нормализует его и проверяет наличие в Google Spreadsheet.

> 📖 **Документация:**
> - [DEPLOY.md](./DEPLOY.md) — **быстрый деплой на Railway** (рекомендуется)
> - [QUICKSTART.md](./QUICKSTART.md) — краткий чеклист
> - [SETUP.md](./SETUP.md) — подробная инструкция
> - [STAGES.md](./STAGES.md) — обзор всех этапов работы

### Технологический стек

- **Node.js (LTS, >= 18)**
- **Telegraf** — Telegram Bot API
- **Express** — health check endpoint
- **dotenv** — загрузка переменных окружения

### Структура проекта

- **src/index.js** — точка входа, запуск бота и HTTP-сервера (health check)
- **src/bot/telegram.js** — логика Telegram-бота
- **src/services/googleSheets.js** — чтение Google Sheets и in-memory кэш
- **src/services/phoneValidator.js** — нормализация и валидация номера
- **src/config/index.js** — конфигурация и работа с переменными окружения

### Переменные окружения

Создайте файл `.env` в корне проекта:

```bash
BOT_TOKEN=your_telegram_bot_token
GOOGLE_SHEET_ID=1LXGrv9ZG8QbkDwJ-cNMeiJj0JaIUGoWve47DEnFkizs
CACHE_TTL_MINUTES=10
PORT=3000
```

### Настройка Google таблицы

Бот работает только с **публичными** Google таблицами (доступными всем по ссылке).

1. Убедитесь, что ваша таблица публичная:
   - Откройте таблицу
   - Нажмите "Настройки доступа" (Share)
   - Установите доступ "Все, у кого есть ссылка" или "Все в интернете"

2. Формат таблицы:
   - Один столбец
   - Первая строка — заголовок `Phone`
   - Начиная со второй строки — номера телефонов, только цифры (например: `79000423366`)

Таблица должна иметь:

- один столбец
- первая строка — заголовок `Phone`
- начиная со второй строки — номера телефонов, только цифры (например: `79000423366`)

### Локальный запуск

```bash
npm install
npm start
```

После запуска:

- бот будет работать по токену `BOT_TOKEN`
- HTTP-сервер будет слушать порт `PORT` (по умолчанию `3000`)
- health check доступен по адресу `/health`

### Логика работы бота

- **/start** — приветствие и краткая инструкция
- **/help** — справка
- **любое текстовое сообщение** — интерпретируется как номер телефона

Ответы:

- номер найден: `✅ Номер найден`
- номер не найден: `❌ Номер не найден`
- некорректный формат: `⚠️ Некорректный формат номера`
- ошибка доступа к таблице: `🔴 Сервис временно недоступен. Попробуйте позже.`

### Деплой на Railway

1. Создайте новый проект на Railway и подключите GitHub-репозиторий.
2. Установите переменные окружения в настройках Railway:
   - `BOT_TOKEN`
   - `GOOGLE_SHEET_ID`
   - `CACHE_TTL_MINUTES` (опционально)
   - `PORT` (Railway обычно сам задаёт)
3. Railway автоматически выполнит `npm install` и команду `npm start`.

После деплоя убедитесь, что:

- endpoint `/health` возвращает JSON со статусом `ok`
- бот отвечает на `/start`, `/help` и проверяет номера по таблице


