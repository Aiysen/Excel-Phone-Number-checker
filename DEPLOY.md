# Деплой на Railway — быстрая инструкция

## ⚠️ Что я могу и не могу сделать

**Я не могу сделать сам:**
- ❌ Создать аккаунт на Railway (нужен ваш аккаунт)
- ❌ Создать GitHub-репозиторий (нужен ваш аккаунт)
- ❌ Получить Bot Token от Telegram (нужен ваш аккаунт)
- ❌ Настроить переменные окружения в Railway (нужен доступ к вашему проекту)

**Я уже сделал:**
- ✅ Весь код готов к деплою
- ✅ Все файлы настроены правильно
- ✅ `package.json` содержит нужные зависимости
- ✅ Код работает с публичными Google таблицами

## 🚀 Что вам нужно сделать (3 простых шага)

### Шаг 1: Подготовка (5 минут)

1. **Создайте Telegram-бота:**
   - Откройте Telegram → найдите @BotFather
   - Отправьте `/newbot`
   - Следуйте инструкциям, получите **Bot Token**
   - Сохраните токен (понадобится в шаге 3)

2. **Проверьте Google таблицу:**
   - Убедитесь, что таблица публичная (доступна всем по ссылке)
   - Формат: первая строка `Phone`, далее номера телефонов

### Шаг 2: GitHub (5 минут)

1. **Создайте репозиторий на GitHub:**
   - Перейдите на https://github.com/new
   - Назовите репозиторий (например: `phone-checker-bot`)
   - Выберите **Public** или **Private** (неважно)
   - **НЕ** добавляйте README, .gitignore или лицензию (уже есть)
   - Нажмите "Create repository"

2. **Загрузите код в GitHub:**
   
   Откройте терминал в папке проекта и выполните:
   
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git
   git push -u origin main
   ```
   
   Замените `ВАШ_USERNAME` и `ВАШ_РЕПОЗИТОРИЙ` на реальные значения.

### Шаг 3: Railway (10 минут)

1. **Создайте проект на Railway:**
   - Перейдите на https://railway.app/
   - Войдите через GitHub (кнопка "Login with GitHub")
   - Нажмите **"New Project"**
   - Выберите **"Deploy from GitHub repo"**
   - Выберите ваш репозиторий из списка
   - Railway автоматически начнёт деплой

2. **Настройте переменные окружения:**
   
   В проекте Railway:
   - Перейдите на вкладку **"Variables"**
   - Нажмите **"New Variable"** и добавьте:
   
   | Имя | Значение |
   |-----|----------|
   | `BOT_TOKEN` | Ваш токен от BotFather |
   | `GOOGLE_SHEET_ID` | `1LXGrv9ZG8QbkDwJ-cNMeiJj0JaIUGoWve47DEnFkizs` |
   | `CACHE_TTL_MINUTES` | `10` (опционально) |
   
   **Важно:** После добавления каждой переменной Railway автоматически перезапустит деплой.

3. **Дождитесь успешного деплоя:**
   - Перейдите на вкладку **"Deployments"**
   - Дождитесь, пока статус станет **"Active"** (зелёная галочка)
   - Проверьте логи: должны быть сообщения:
     - `✅ Кэш Google Sheets загружен`
     - `✅ Telegram бот запущен`
     - `✅ HTTP сервер запущен`

4. **Проверьте работу:**
   - Откройте вкладку **"Settings" → "Networking"**
   - Скопируйте домен (например: `your-project.railway.app`)
   - Откройте в браузере: `https://your-project.railway.app/health`
   - Должен вернуться JSON со статусом `ok`
   - Проверьте бота в Telegram: отправьте `/start`

## ✅ Готово!

Бот работает на Railway и доступен 24/7.

## 🔍 Проверка работы

Отправьте боту в Telegram:

1. `/start` — должно прийти приветствие
2. `/help` — должна прийти справка  
3. `79000423366` — проверка номера
4. `+7 900 042 33 66` — проверка нормализации

## ❓ Проблемы?

**Бот не отвечает:**
- Проверьте логи в Railway (Deployments → View Logs)
- Убедитесь, что `BOT_TOKEN` указан правильно

**Ошибка загрузки таблицы:**
- Проверьте, что таблица публичная
- Проверьте `GOOGLE_SHEET_ID` в переменных окружения

**Деплой не запускается:**
- Проверьте логи в Railway
- Убедитесь, что все переменные окружения добавлены

## 📝 Краткая памятка

```
1. Telegram → @BotFather → /newbot → получить токен
2. GitHub → создать репозиторий → загрузить код
3. Railway → New Project → выбрать репозиторий → добавить переменные → готово!
```

**Время: ~20 минут**

